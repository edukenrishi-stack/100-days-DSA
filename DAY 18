#include <stdio.h>

// Helper function to reverse a portion of the array
void reverse(int arr[], int start, int end) {
    while (start < end) {
        int temp = arr[start];
        arr[start] = arr[end];
        arr[end] = temp;
        start++;
        end--;
    }
}

int main() {
    int n, k, i;

    // Input size
    if (scanf("%d", &n) != 1) return 0;

    int arr[n];
    // Input array elements
    for (i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    // Input rotation steps
    scanf("%d", &k);

    // If k is greater than n, we only need to rotate k % n times
    k = k % n;

    if (k > 0) {
        // Step 1: Reverse the whole array
        reverse(arr, 0, n - 1);
        // Step 2: Reverse first k elements
        reverse(arr, 0, k - 1);
        // Step 3: Reverse the rest
        reverse(arr, k, n - 1);
    }

    // Print the result
    for (i = 0; i < n; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");

    return 0;
}
